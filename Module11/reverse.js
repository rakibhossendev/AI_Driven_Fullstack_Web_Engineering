// System 1

const message = "Hello I am,";

const messageArray = message.split('');
const reversed = messageArray.reverse();
const reversedMessage = messageArray.join('');
console.log(reversedMessage)