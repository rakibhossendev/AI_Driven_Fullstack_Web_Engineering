export default function BlogLayout({children}:{children: React.ReactNode}){

    return (    
        <section>
            <h2>Layouts</h2>
            {children}
            <div>

            </div>
        </section>
    )
}